package player;

import java.awt.Point;
import framework.Board;

public class Player {
    public Point getMove(Board b) {
        if (b.getTurnCount() % 2 == 0) {
            
        }
        return new Point(1, 1);
    }
}
